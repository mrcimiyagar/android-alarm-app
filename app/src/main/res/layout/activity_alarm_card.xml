<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent"
    tools:context="kasper_external_apps.android.alarmer.front.activities.AlarmCardActivity">

    <kasper_external_apps.android.alarmer.front.components.CustomTextView
        android:id="@+id/activity_alarm_card_details_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="112dp"
        android:layout_marginTop="-64dp"
        android:layout_marginLeft="16dp"
        android:layout_marginRight="16dp"
        android:background="@drawable/background_container"
        android:layout_centerInParent="true"
        android:elevation="4dp"
        android:paddingTop="64dp"
        android:paddingBottom="64dp"
        android:textColor="#000"
        android:paddingLeft="16dp"
        android:paddingRight="16dp"
        android:textSize="18sp"
        android:text="صبح یادت نره بری دانشگاه"/>

    <kasper_external_apps.android.alarmer.front.components.CustomTextView
        android:id="@+id/activity_alarm_card_title_text_view"
        android:layout_width="wrap_content"
        android:layout_height="64dp"
        android:background="@drawable/background_floating_text"
        android:layout_margin="32dp"
        android:text="بیدار شو"
        android:layout_centerHorizontal="true"
        android:layout_above="@+id/activity_alarm_card_details_text_view"
        android:paddingLeft="24dp"
        android:paddingRight="24dp"
        android:gravity="center"
        android:textColor="#fff"
        style="@style/TextAppearance.AppCompat.Widget.ActionBar.Title"
        android:elevation="6dp"/>

    <android.support.design.widget.FloatingActionButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:fabSize="normal"
        app:backgroundTint="@color/colorAccent"
        android:tint="#fff"
        android:src="@drawable/done"
        android:layout_marginTop="-28dp"
        android:layout_below="@+id/activity_alarm_card_details_text_view"
        android:layout_centerHorizontal="true"
        android:onClick="onOkBtnClicked"/>

</RelativeLayout>
